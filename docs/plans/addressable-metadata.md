# Plan — Addressable metadata refactor (CMA run)

Generated by the CMA planner (Fable). Raw task graph: [addressable-metadata-task-graph.json](addressable-metadata-task-graph.json)

## Objective

Finish wiring the addressable-metadata refactor: make key-mapped fields (metadata_keys.harvest + field_map.resolve_fields) the PRIMARY source in metadata.py with the legacy regex scanners demoted to fallback, add a profile-level field_key_map override threaded through service.py, emit a per-series sidecar that can never enter BatchResult.planned, and expose the harvested keys + per-field key provenance in the Streamlit UI. All Python runs via .venv/Scripts/python.exe; fast tests (83 now) stay green and ruff/black (line-length 100) stay clean after every task.

## Assumptions verified during planning

- Verified live during planning (metadata-only, no pixel access): harvest() on C:\Users\Owner\Desktop\sandbox\micronaut\GnRH1-GFP_42dpf_fish1_20072026.lif returns 4 series with 95 keys each in 0.5s; resolve_fields gives X40 for 'closeup_cluster_left_x40_res=1024x1024_zoom=1.1' and X20 for the other three; _shared_fields correctly OMITS magnification and yields markers only.
- IMPORTANT for verification wording: the container-level shared markers STRING is 'DAPI-CY3-ALEXA488' (series-0's detector order), while series 2 and 3 individually resolve to 'ALEXA488-DAPI-CY3'. These are set-equal and _shared_fields compares markers as a SET by design. Every acceptance check on markers MUST compare set(value.split('-')) == {'ALEXA488','DAPI','CY3'}, never a literal string, or it will falsely fail.
- NEWLY FOUND, load-bearing: BioImage(lif).channel_names == ['Green','Blue','Red'] (LUT display colours) and _is_placeholder_channel does NOT filter colour names. metadata.py line ~527 does an UNCONDITIONAL result['markers'] = '-'.join(named_channels), so wiring the key map without demoting that block still produces GREEN-BLUE-RED. This is the second half of the proven bug and T1 must fix it.
- NEWLY FOUND: _detect_format returns 'GENERIC' for a plain .tif (only .ome.tif maps to OME-TIFF), so a Fiji TIFF would route to _harvest_generic and lose the ImageJ Info block, breaking tests/test_metadata.py's 3 integration tests (all 3 pass today). DEFAULT_FIELD_MAPS already contains a 'TIFF' entry and _HARVESTERS already contains 'TIFF', so returning 'TIFF' for .tif/.tiff is the intended wiring.
- BioImage construction costs ~6.9s on the 1.02GB LIF while harvest costs 0.5s; harvest for LIF uses readlif and does not need bioio at all, so harvest must run BEFORE BioImage and a BioImage failure must not discard harvested keys.
- All 7 existing test fakes are declared as `def fake_extract_metadata_detailed(file_path, **kwargs)`, so adding a `field_key_map` keyword to extract_metadata_detailed is absorbed without touching them; they also expect a 3-tuple return, so ExtractionDetail may only gain fields, never change arity.
- config.template already contains {notes}, and DEFAULT_FIELD_MAPS['LIF'] maps notes->SeriesName, so a SINGLE-series LIF will now carry its series name in the filename's notes slot; multi-series containers omit it because the names disagree. Treated as intended behaviour, not a defect.
- Environment verified: .venv has readlif 0.6.6, bioio_lif, tifffile; `pytest -q -m "not integration"` = 83 passed, `pytest -q tests/test_metadata.py -m integration` = 3 passed.
- Executors must not run git commit/push; the orchestrator owns git. A dirty working tree carrying earlier verified tasks' edits is expected, not a defect.

## Tasks

| ID | Title | Depends on | Parallel |
|----|-------|-----------|----------|
| T1 | Make key-mapped fields the primary source in _read_with_bioio; demote regex + channel_names to fallback | — | yes |
| T2 | Add field_key_map to ProfileRules across all 4 profiles.py touchpoints + legacy-load test | — | yes |
| T3 | Thread profile.field_key_map into extraction and surface key provenance on SuggestionResult | T1, T2 | yes |
| T4 | Carry per-image (per-series) resolved field records through the spawn Queue into ExtractionDetail | T1 | yes |
| T5 | service.build_series_rows: per-series sidecar rows that can never enter BatchResult.planned | T3, T4 | no |
| T6 | Generalize cli.py's report writer over arbitrary columns and add `batch --sidecar` | T5 | no |
| T7 | app_streamlit.py: drop the duplicated CSV writer and add the per-series sidecar download | T6 | no |
| T8 | app_streamlit.py: searchable key table, field->key mapping dropdowns, and per-field key provenance | T7, T2 | no |

## Execution batches

1. T1 + T2
2. T3 + T4
3. T5
4. T6
5. T7
6. T8

## Task detail

### T1 — Make key-mapped fields the primary source in _read_with_bioio; demote regex + channel_names to fallback

**Scope**

EDIT ONLY g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\src\microscopy_naming_assistant\metadata.py.
(1) _detect_format: return "TIFF" for .tif/.tiff (keep .ome.tif/.ome.tiff -> "OME-TIFF"). Without this a Fiji TIFF routes to _harvest_generic and the ImageJ Info block is lost; _HARVESTERS and DEFAULT_FIELD_MAPS already have a "TIFF" entry.
(2) _read_with_bioio(file_path, field_key_map=None): call `images = harvest(file_path, file_format)` FIRST, before any BioImage construction (harvest never raises; LIF harvest uses readlif and needs no bioio, and BioImage costs ~6.9s on the 1.02GB LIF). Build metadata_text = _render_metadata_text(images); for file_format in ("TIFF","OME-TIFF") append the existing _collect_metadata_text(image, file_path) ImageJ shim text so raw tags stay visible.
(3) PRIMARY: fields, provenance = _shared_fields(images, file_format, field_key_map). FALLBACK: for each of markers/magnification/exptype/sample/date NOT already produced by the map, run the existing _extract_markers/_extract_magnification/_extract_exptype/_extract_sample/_extract_acquisition_date over _extract_text_chunks(metadata_text) exactly as today. Do not delete those scanners.
(4) CRITICAL: the `channel_names` block must become a fallback used ONLY when `markers` is still unset. Proven this session: BioImage(the real LIF).channel_names == ['Green','Blue','Red'] (LUT colours) and _is_placeholder_channel does not filter colour names, so today's unconditional `result["markers"] = "-".join(named_channels)` overwrites the correct dyes with GREEN-BLUE-RED.
(5) Wrap the BioImage construction (reader name + channel fallback) in its OWN try/except so a bioio failure records `error` but never discards the harvested keys/metadata_text.
(6) Payload gains "key_paths": dict(images[0].keys) if images else {}, and "field_key_provenance": the provenance dict from _shared_fields. Both plain str->str dicts, hence picklable across the spawn Queue.
(7) _bioio_worker(file_path_str, q, field_key_map=None) and ctx.Process(target=_bioio_worker, args=(str(file_path), q, field_key_map)); its except branch must still put a payload containing the new keys so the parent never blocks.
(8) ExtractionDetail: ADD key_paths: dict[str,str] = field(default_factory=dict) and field_key_provenance: dict[str,str] = field(default_factory=dict) (import `field` from dataclasses). Do NOT change or remove existing fields — 7 test fakes construct ExtractionDetail() and expect a 3-tuple return from extract_metadata_detailed.
(9) extract_metadata_detailed gains `field_key_map: dict[str,str] | None = None`, forwards it to the worker, and populates the two new detail fields from the payload. Keep source tagging unchanged (bioio-supplied keys -> "metadata").
INVARIANT — DO NOT TOUCH: the parent reads `q.get(timeout=...)` BEFORE `p.join(...)`. Reordering deadlocks on any payload larger than the ~64KB pipe buffer. Keep the terminate->join->kill escalation as-is.
INVARIANT: metadata_text must stay populated — llm.py, cli.py --show-metadata/--json and app_streamlit.py all consume it.
Never call .data / .get_image_data / .xarray_data on the 1.02GB LIF.

**Verification**

Run all four, all with .venv/Scripts/python.exe:
(a) `.venv/Scripts/python.exe -m pytest -q -m "not integration"` -> 83 passed.
(b) `.venv/Scripts/python.exe -m pytest -q tests/test_metadata.py -m integration` -> 3 passed (these cover the ImageJ Info block + detail.reader/metadata_text; they pass today and prove the .tif->TIFF routing did not regress).
(c) Write a throwaway script under the scratchpad (metadata only; never .data/.get_image_data/.xarray_data) that calls extract_metadata_detailed(Path(r"C:\Users\Owner\Desktop\sandbox\micronaut\GnRH1-GFP_42dpf_fish1_20072026.lif"), timeout_seconds=60) and asserts ALL of: `"magnification" not in fields` (the 4 series genuinely disagree, 20 vs 40, so the container must claim nothing); `set(fields["markers"].split("-")) == {"ALEXA488","DAPI","CY3"}` (compare as a SET — the container string is 'DAPI-CY3-ALEXA488'); `"GREEN" not in fields["markers"]` (LUT-colour regression gate); `sources["markers"] == "metadata"`; `detail.field_key_provenance["markers"] == "Dyes"`; `len(detail.key_paths) >= 90 and "Magnification" in detail.key_paths` (proves the dicts survived the spawn Queue); `"Magnification = 20" in detail.metadata_text`; `detail.timed_out is False`. Script must exit 0.
(d) In the same script, per-series: for im in harvest(path,"LIF"): resolve_fields(im,"LIF",None)[0]["magnification"] == "X40" for the series whose name starts 'closeup_cluster_left' and "X20" for the other three.
(e) `.venv/Scripts/python.exe -m ruff check src/microscopy_naming_assistant/metadata.py` and `.venv/Scripts/python.exe -m black --check src/microscopy_naming_assistant/metadata.py` both clean (line-length 100). The previously-unused `harvest` import must now be used.

### T2 — Add field_key_map to ProfileRules across all 4 profiles.py touchpoints + legacy-load test

**Scope**

EDIT g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\src\microscopy_naming_assistant\profiles.py and g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\tests\test_config_profiles.py.
Follow the EXACT pattern filename_extraction_mask uses, all four touchpoints in profiles.py:
(a) ProfileRules dataclass: `field_key_map: dict[str, str] = field(default_factory=dict)` (import `field` from dataclasses; it must stay AFTER the existing defaulted fields so positional construction in app_streamlit.py's profile wizard is unaffected).
(b) default_profile(): pass field_key_map={} explicitly.
(c) save_profile(): add "field_key_map": profile.field_key_map to the hand-written payload dict. THIS IS THE TRAP — the payload is hand-written, NOT asdict(), so omitting the key silently drops the field on every save.
(d) load_profile(): field_key_map=payload.get("field_key_map", base.field_key_map).
Semantics: maps a naming field name (one of field_map.MAPPABLE_FIELDS) to an exact metadata key name; consumed by field_map.resolve_fields as `overrides`. Do not import field_map here (avoid a new import cycle) — profiles.py stays dependency-free.
Tests to add in tests/test_config_profiles.py, mirroring the existing ones:
- extend/add a round-trip test saving field_key_map={"magnification": "Magnification"} and asserting it loads back;
- assert the RAW json on disk contains the key: `json.loads(path.read_text())["field_key_map"] == {...}` — this is what catches the hand-written-payload omission that a dataclass round trip alone would hide;
- a legacy test mirroring test_profile_without_extraction_mask_loads: write '{"name": "legacy"}' and assert load_profile(path).field_key_map == {}.
Do not touch service.py (that is T3).

**Verification**

`.venv/Scripts/python.exe -m pytest -q -m "not integration"` -> 85 passed (83 existing + 2 new), zero failures. Additionally `.venv/Scripts/python.exe -c "import sys;sys.path.insert(0,'src');import json,tempfile,pathlib;from microscopy_naming_assistant.profiles import default_profile,save_profile,load_profile;p=pathlib.Path(tempfile.mkdtemp())/'p.json';pr=default_profile();pr.field_key_map={'magnification':'ObjectiveName'};save_profile(p,pr);raw=json.loads(p.read_text());assert 'field_key_map' in raw and raw['field_key_map']=={'magnification':'ObjectiveName'};assert load_profile(p).field_key_map=={'magnification':'ObjectiveName'};print('OK')"` prints OK. ruff + black clean on both edited files.

### T3 — Thread profile.field_key_map into extraction and surface key provenance on SuggestionResult

**Scope**

EDIT g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\src\microscopy_naming_assistant\service.py and add tests to g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\tests\test_service_and_cli.py.
This is touchpoint (e) of the 5-touchpoint field_key_map pattern.
(1) suggest_for_file: pass `field_key_map=profile.field_key_map if profile else None` to extract_metadata_detailed, alongside the existing extraction_mask argument.
(2) SuggestionResult gains `key_paths: dict[str, str] = field(default_factory=dict)` and `field_key_provenance: dict[str, str] = field(default_factory=dict)`, populated from detail. ADD ONLY — existing fields, order of the already-defaulted fields, and every existing construction site (tests build SuggestionResult with keyword args) must keep working.
(3) Do not change plan_batch/recalculate_batch/apply_batch in this task.
INVARIANT to restate in code comments: BatchResult.planned must stay strictly 1:1 with real filesystem entries — apply_batch does an unguarded src.rename(dst) in a loop, so two results sharing one source path raise FileNotFoundError on the second iteration AND skip save_manifest, leaving the first rename unrollbackable.
Tests to add: a monkeypatched extract_metadata_detailed (signature `(file_path, **kwargs)`) that records kwargs, proving (i) field_key_map arrives with the profile's dict when a profile_path is given and (ii) it is None when no profile is given; and one asserting SuggestionResult.field_key_provenance/key_paths are copied from the returned ExtractionDetail.

**Verification**

`.venv/Scripts/python.exe -m pytest -q -m "not integration"` -> 88 passed (85 + 3 new), zero failures — in particular the 7 pre-existing fakes that monkeypatch service.extract_metadata_detailed and return a 3-tuple must still pass untouched. Then `.venv/Scripts/python.exe -c "import sys;sys.path.insert(0,'src');import inspect;from microscopy_naming_assistant import service;src=inspect.getsource(service.suggest_for_file);assert 'field_key_map' in src;print('OK')"` prints OK. ruff + black clean on both files.

### T4 — Carry per-image (per-series) resolved field records through the spawn Queue into ExtractionDetail

**Scope**

EDIT ONLY g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\src\microscopy_naming_assistant\metadata.py (builds on T1; expect an already-modified file and do not revert T1's work).
(1) Add a helper `_per_image_records(images, file_format, overrides) -> list[dict[str, str]]` that calls field_map.resolve_fields once per image and returns, per image, a FLAT dict of strings: {"index": str(image.index), "name": image.name, **resolved_fields}. Everything str->str so the list pickles cleanly across the multiprocessing spawn Queue. Refactor so resolve_fields is not called twice per image if that is cheap to do (_shared_fields already resolves them) — but correctness first; _shared_fields' existing behaviour (markers compared as a SET, disagreeing fields omitted) must not change.
(2) _read_with_bioio payload gains "images": that list.
(3) ExtractionDetail gains `images: list[dict[str, str]] = field(default_factory=list)`, populated from the payload in extract_metadata_detailed. ADD ONLY; the 3-tuple return arity and existing fields stay identical.
(4) The _bioio_worker except branch must include "images": [] so a crashing child still produces a well-formed payload.
INVARIANT — DO NOT TOUCH: q.get() still happens BEFORE p.join(); this list makes the payload bigger, which is exactly the case where reordering would deadlock on the ~64KB pipe buffer.
This task must NOT change container-level `fields`/`sources` at all — it is purely additive detail for the sidecar.
Never load pixel data from the 1.02GB LIF.

**Verification**

(a) `.venv/Scripts/python.exe -m pytest -q -m "not integration"` -> same count as after T2/T3's additions, zero failures; `.venv/Scripts/python.exe -m pytest -q tests/test_metadata.py -m integration` -> 3 passed.
(b) Re-run T1's real-LIF script unchanged: every T1 assertion (magnification absent, markers set == {ALEXA488,DAPI,CY3}, provenance 'Dyes', >=90 key_paths) must still hold — this is the no-regression gate.
(c) Extend that script to assert on the SAME extract_metadata_detailed(...) return (i.e. values that actually crossed the spawn Queue, which is the picklability proof): len(detail.images) == 4; the four `name` values equal the four series names; the record whose name starts 'closeup_cluster_left' has magnification == 'X40'; the other three have magnification == 'X20'; every value in every record is a `str`. Script exits 0.
(d) ruff + black clean on metadata.py.

### T5 — service.build_series_rows: per-series sidecar rows that can never enter BatchResult.planned

**Scope**

EDIT g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\src\microscopy_naming_assistant\service.py and add tests to g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\tests\test_service_and_cli.py.
Add `build_series_rows(suggestions: list[SuggestionResult], config: NamingConfig) -> list[dict[str, str]]` returning rows with EXACTLY these keys in this order: source, series_index, internal_name, suggested_name.
Per suggestion: skip unless len(result.detail images) > 1 (a single-image file has nothing extra to say — the ordinary rename already covers it). For each per-image record, overlay the record's field values (excluding the 'index'/'name' bookkeeping keys) onto the container's `result.fields`, then use the existing naming.finalize_fields + naming.render_name to produce suggested_name. source = result.source.name; series_index = the record's index; internal_name = the record's name.
To do this, SuggestionResult must carry the per-image records: add `images: list[dict[str, str]] = field(default_factory=list)` populated from detail.images in suggest_for_file (additive, same pattern as T3's fields).
SAFETY CONSTRAINT — the whole point of this task, state it in the function docstring: per-series entries are SIDECAR-ONLY. They must never be appended to BatchResult.planned and no per-series rename or pixel export happens. apply_batch does an unguarded src.rename(dst) in a loop, so N rows sharing one source path would raise FileNotFoundError on iteration 2 AND skip save_manifest, leaving the first rename unrollbackable. build_series_rows must therefore be a pure function that neither takes nor mutates a BatchResult; do not modify recalculate_batch, plan_batch or apply_batch.
Do not touch cli.py or app_streamlit.py (T6/T7).

**Verification**

`.venv/Scripts/python.exe -m pytest -q -m "not integration"` -> all prior tests plus the new ones pass, zero failures. New tests must include:
(a) a SuggestionResult carrying 4 per-image records (two at X20, one at X40, distinct internal names) yields exactly 4 rows, every row's `source` is the same container filename, row keys are exactly ['source','series_index','internal_name','suggested_name'], and the suggested_name for the X40 record differs from the X20 ones (assert 'X40' in it and 'X20' in another);
(b) a single-image SuggestionResult yields 0 rows;
(c) THE SAFETY GATE: build a batch via recalculate_batch from that same 4-series suggestion and assert `len(batch.planned) == 1` and `[src for src, _ in batch.planned] == [suggestion.source]` — i.e. the per-series rows did not leak into planned;
(d) `.venv/Scripts/python.exe -c "import sys;sys.path.insert(0,'src');import inspect;from microscopy_naming_assistant import service;assert 'planned' not in inspect.signature(service.build_series_rows).parameters;print('OK')"` prints OK.
ruff + black clean on both files.

### T6 — Generalize cli.py's report writer over arbitrary columns and add `batch --sidecar`

**Scope**

EDIT g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\src\microscopy_naming_assistant\cli.py and add tests to g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\tests\test_service_and_cli.py.
(1) Replace the hardcoded `fieldnames=["source","target","issues"]` with column names derived from the first row's keys (dicts preserve insertion order, so build rows in the intended column order). Extract a PUBLIC `rows_to_csv(rows: list[dict[str, str]]) -> str` returning the CSV text (used by app_streamlit.py in T7 so the duplicated writer there can be deleted) and keep a `_write_rows(path, rows)` that writes JSON when the path ends in .json and CSV otherwise. Generalize the EXISTING writer — do not add a second CSV writer anywhere.
REGRESSION GATE: the rename report's output must stay byte-identical — header exactly `source,target,issues` and a row exactly `a.tif,A.tif,warning:sample`; two existing tests assert this and must pass unmodified.
(2) Add `--sidecar PATH` to the `batch` subparser (help: per-series CSV/JSON for multi-image containers such as LIF/ND2/CZI; JSON if the path ends in .json). After planning, write service.build_series_rows(batch.suggestions, config) through the same writer. Load the config the same way cmd_batch already resolves it. If the row list is empty, write nothing and (non-JSON mode only) print that no multi-image containers were found. Print `Sidecar written to: {path}` only in non-JSON mode, mirroring how `--report` is handled; in --json mode add a "sidecar" entry to the printed payload instead.
SAFETY: --sidecar must not influence batch.planned, apply_batch, or the manifest in any way — it is a pure side report.

**Verification**

`.venv/Scripts/python.exe -m pytest -q -m "not integration"` -> all pass, and specifically test_cmd_batch_report_csv_writes_source_target_issues and test_cmd_batch_report_json_parses_to_expected_list pass WITHOUT being edited (byte-identical report gate). New tests: (a) `batch --sidecar out.csv` with a monkeypatched plan_batch returning one suggestion carrying 4 per-image records writes a file whose first line is exactly `source,series_index,internal_name,suggested_name` with 4 data rows; (b) `--sidecar out.json` parses to a 4-element list of dicts with those 4 keys; (c) a suggestion with no multi-image records writes no sidecar file and the command still exits 0; (d) `batch --sidecar ... --apply` still renames exactly 1 file and writes a manifest (proves the sidecar never entered planned). Also `.venv/Scripts/python.exe -c "import sys;sys.path.insert(0,'src');from microscopy_naming_assistant.cli import rows_to_csv;print(repr(rows_to_csv([{'a':'1','b':'2'}])))"` prints a header line 'a,b'. ruff + black clean on both files.

### T7 — app_streamlit.py: drop the duplicated CSV writer and add the per-series sidecar download

**Scope**

EDIT ONLY g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\app_streamlit.py (around lines 360-400, the Planned Renames / download-report block).
(1) Replace the duplicated `csv.StringIO` + `csv.DictWriter(csv_buffer, fieldnames=["source","target","issues"])` block with `rows_to_csv` imported from microscopy_naming_assistant.cli (a local import at the point of use matches this file's existing style). The downloaded rename report's content must be unchanged: same rows, same `source,target,issues` header, same file_name/mime/key on both existing download buttons. Prefer reusing cli._batch_report_rows for the row construction so the CSV and JSON downloads and the CLI --report all agree.
(2) Add "Download per-series sidecar (CSV)" and "(JSON)" buttons built from service.build_series_rows(batch.suggestions, config), with distinct `key=` values (e.g. download_sidecar_csv/json) so Streamlit does not raise duplicate-key errors. When the row list is empty, render a short caption explaining that no multi-image containers were found instead of an empty download.
SAFETY — do not touch the Apply Renames path: it must keep calling apply_batch(input_dir, batch.planned) with planned strictly 1:1 with real files. Per-series rows are download-only; never rename per series and never export pixel data.
Respect the file's per-file ruff ignore (E402 is expected here).
Do NOT change the 'Metadata read from files' expander in this task — that is T8.

**Verification**

(a) `.venv/Scripts/python.exe -c "import ast;ast.parse(open(r'app_streamlit.py',encoding='utf-8').read());print('parsed')"` prints parsed.
(b) `.venv/Scripts/python.exe -c "import sys;sys.path.insert(0,'src');from microscopy_naming_assistant.cli import rows_to_csv;from microscopy_naming_assistant.service import build_series_rows;print('imports ok')"` prints imports ok (proves the symbols the page imports actually exist).
(c) grep proof: `grep -n 'DictWriter' app_streamlit.py` returns NO matches (the duplicate writer is gone), and `grep -n 'apply_batch(input_dir, batch.planned)' app_streamlit.py` still matches (Apply path untouched), and `grep -c 'download_sidecar' app_streamlit.py` >= 2.
(d) `.venv/Scripts/python.exe -m pytest -q -m "not integration"` still fully green.
(e) ruff check + black --check on app_streamlit.py clean.

### T8 — app_streamlit.py: searchable key table, field->key mapping dropdowns, and per-field key provenance

**Scope**

EDIT ONLY g:\My Drive\FACSI\Image Analysis\github-desktop-repos\microscopy-naming-assistant\app_streamlit.py, inside the existing `with st.expander("Metadata read from files", expanded=False):` block (~line 337). Builds on T7; expect an already-modified file.
(1) Below the existing reader/error/st.code display, render the harvested keys as a SEARCHABLE table: a st.text_input filter (key="metadata_key_filter") plus st.dataframe of [{"key": k, "value": v} for k, v in sorted(selected.key_paths.items()) if the filter substring matches k or v, case-insensitive]. Show the key count. If key_paths is empty, say so instead of rendering an empty grid.
(2) Per-field provenance: for each entry in selected.field_key_provenance, show `field = value  (from key: <key>)` so the user sees WHICH metadata key produced each value. This is the whole point of the refactor — a value that says only 'metadata' is what let a 40x objective be reported as X1.
(3) Field -> key mapping: one st.selectbox per naming field in field_map.MAPPABLE_FIELDS, options = ["(automatic)"] + sorted(selected.key_paths), preselected from the currently loaded profile's field_key_map (falling back to "(automatic)"), each with a distinct key= (e.g. f"fieldmap_{field}"). A "Save mapping to profile" button then load_profile(profile_path) -> set profile.field_key_map to {field: key for chosen entries that are not "(automatic)"} -> save_profile(profile_path, profile) -> st.success, and st.rerun() so the next extraction picks it up. When no profile path is configured, show an st.info explaining a profile must be selected/created first rather than silently doing nothing; wrap the save in try/except and surface the error with st.error.
Respect the per-file E402 ruff ignore and this file's local-import style.
Do not alter the Planned Renames / download / Apply blocks from T7.

**Verification**

(a) `.venv/Scripts/python.exe -c "import ast;ast.parse(open(r'app_streamlit.py',encoding='utf-8').read());print('parsed')"` prints parsed.
(b) grep proof of all three features: `grep -c 'metadata_key_filter' app_streamlit.py` >= 1; `grep -c 'field_key_provenance' app_streamlit.py` >= 1; `grep -n 'MAPPABLE_FIELDS' app_streamlit.py` matches; `grep -n 'save_profile' app_streamlit.py` shows a call inside the metadata expander in addition to the pre-existing profile wizard one.
(c) Prove the override the dropdown writes is actually honoured end-to-end, without Streamlit: a scratchpad script (metadata only, never .data/.get_image_data/.xarray_data) that runs `harvest(Path(r"C:\Users\Owner\Desktop\sandbox\micronaut\GnRH1-GFP_42dpf_fish1_20072026.lif"), "LIF")` and asserts resolve_fields(images[2], "LIF", {"magnification": "ObjectiveName"})[1]["magnification"] == "ObjectiveName" while resolve_fields(images[2], "LIF", None)[1]["magnification"] == "Magnification"; exits 0.
(d) A round-trip check that the UI's save path persists: save a profile with field_key_map via profiles.save_profile and assert load_profile returns it (already covered by T2's test suite — rerun `.venv/Scripts/python.exe -m pytest -q -m "not integration"`, fully green).
(e) ruff check + black --check on app_streamlit.py clean.

## Risks

- **Risk:** HIGHEST — bioio's channel_names for the real LIF is ['Green','Blue','Red'] (LUT display colours) and today's code assigns them to markers UNCONDITIONALLY, so simply wiring the key map still yields GREEN-BLUE-RED. _is_placeholder_channel does not filter colour names.
  - **Mitigation:** T1 scope names the exact line and demotes the channel_names block to a fallback used only when markers is still unset; T1 verification asserts `"GREEN" not in fields["markers"]` and set-equality with {ALEXA488,DAPI,CY3} against the real file. T4 re-runs the same script as a no-regression gate.
- **Risk:** _detect_format returns GENERIC for a plain .tif, so routing harvest by file_format would send Fiji TIFFs to _harvest_generic and lose the ImageJ Info block — silently reverting the previously-fixed 'stripped TIFF names as UNKNOWN' bug. The 3 affected tests are marked integration and are DESELECTED by the default fast run, so this would not show up in `pytest -m "not integration"`.
  - **Mitigation:** T1 changes _detect_format to return 'TIFF' for .tif/.tiff (both _HARVESTERS and DEFAULT_FIELD_MAPS already have a TIFF entry) and T1/T4 verification explicitly runs `pytest -q tests/test_metadata.py -m integration` expecting 3 passed, not just the fast suite.
- **Risk:** Per-series rows leaking into BatchResult.planned would make apply_batch throw FileNotFoundError on the second row sharing a source path AND skip save_manifest, leaving the first rename unrollbackable — silent data loss.
  - **Mitigation:** Sidecar-only by construction: build_series_rows is a pure function that never receives a BatchResult (T5 asserts its signature has no 'planned' parameter); T5 has an explicit gate asserting len(batch.planned)==1 for a 4-series container; T6 verifies `--sidecar --apply` renames exactly 1 file and still writes a manifest; T7 greps that the Apply button still passes batch.planned.
- **Risk:** The multiprocessing spawn Queue payload grows (95-key dict + 4 per-image records). An unpicklable value or a reordered read/join would deadlock or silently drop the payload on exactly the metadata-rich files this refactor targets.
  - **Mitigation:** Every added value is a plain str->str dict or a list of them; T1 and T4 scopes forbid touching the q.get()-before-p.join() ordering and the terminate->join->kill escalation; verification asserts the new fields' CONTENTS after they crossed the queue (>=90 key_paths, 4 image records, all values str), which is the real picklability proof rather than a code read. The worker's except branch must also emit the new keys so a crashing child never blocks the parent.
- **Risk:** save_profile's payload dict is hand-written rather than asdict(), so omitting field_key_map there would silently drop every saved mapping while a dataclass-level round-trip test still passed — the UI dropdown in T8 would appear to work and forget on reload.
  - **Mitigation:** T2 verification asserts the key is present in the RAW json.loads() of the written file, not just on the reloaded dataclass, and adds a legacy-load test mirroring test_profile_without_extraction_mask_loads.
- **Risk:** Generalizing cli._write_batch_report's hardcoded fieldnames could reorder or rename the rename-report columns, breaking the two existing report tests and any user script consuming that CSV.
  - **Mitigation:** T6 treats it as a byte-level regression gate: header must stay exactly `source,target,issues` and the sample row exactly `a.tif,A.tif,warning:sample`, with both existing tests passing UNMODIFIED. T7 then deletes app_streamlit.py's duplicate writer and routes it through the same rows_to_csv so the two cannot drift again.
- **Risk:** Behaviour change for users: config.template contains {notes} and DEFAULT_FIELD_MAPS['LIF'] maps notes->SeriesName, so a single-series LIF's suggested filename will newly carry the series name in the notes slot.
  - **Mitigation:** Intended and safe by design — _shared_fields omits notes whenever series names disagree, so multi-series containers are unaffected, and per-series notes go to the sidecar. T1's real-LIF verification asserts the 4-series container's field set explicitly, so any surprise here surfaces at the plan's first gate rather than in a user's filenames.
- **Risk:** BioImage construction costs ~6.9s on the 1.02GB LIF and today an exception there aborts the whole read, discarding harvested keys; combined with spawn import overhead this can approach the default 20s worker timeout.
  - **Mitigation:** T1 runs harvest (0.5s, readlif, no bioio) BEFORE BioImage and wraps the BioImage call in its own try/except so a failure or slow path records `error` without losing metadata_text/key_paths; verification calls extract_metadata_detailed with timeout_seconds=60 and asserts detail.timed_out is False.
