# Fluorophore library picker

Replace the structured Color-panel fluorophore text field with a complete spectrum-library select. Keep one explicit custom option for entries absent from the library, and save both known and custom values through the existing `panel.channels` autosave path.

The authoritative scope and verification contract is `planner-web-fluorophore-picker-task-graph.json`.

Status: complete and independently verified. Publication remains pending explicit commit/push approval for this uncommitted UI wave.
